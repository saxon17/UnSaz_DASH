# -*- mode: python -*-
a = Analysis(['xlGUI.py'],
             pathex=['F:\\BaiduYunDownload\\dash 720p\\xl2MySql_GUI'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='xlGUI.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='xlGUI')
